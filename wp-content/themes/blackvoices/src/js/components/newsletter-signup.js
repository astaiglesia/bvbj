/**
* Initializes the site's newsletterSignup component.
* @constructor
* @param {Object} el - The site's newsletterSignup container element.
*/
const newsletterSignup = el => {
    console.log(el)
}
export default function(el) {
    newsletterSignup(el)
}
