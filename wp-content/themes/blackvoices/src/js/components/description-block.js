/**
* Initializes the site's descriptionBlock component.
* @constructor
* @param {Object} el - The site's descriptionBlock container element.
*/
const descriptionBlock = el => {
    console.log(el)
}
export default function(el) {
    descriptionBlock(el)
}
