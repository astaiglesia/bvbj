/**
* Initializes the site's footer component.
* @constructor
* @param {Object} el - The site's footer container element.
*/
const footer = el => {
    console.log(el)
}
export default function(el) {
    footer(el)
}
