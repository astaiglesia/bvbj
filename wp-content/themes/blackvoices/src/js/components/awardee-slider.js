/**
* Initializes the site's awardeeSlider component.
* @constructor
* @param {Object} el - The site's awardeeSlider container element.
*/
const awardeeSlider = el => {
    console.log(el)
}
export default function(el) {
    awardeeSlider(el)
}
